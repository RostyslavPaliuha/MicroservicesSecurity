server:
  port: 8761
spring:
  boot:
    admin:
      client:
        url: "http://localhost:9000"

management:
  endpoints:
    web:
      exposure:
        include: "*"
    jmx:
      exposure:
        include: "*"
  endpoint:
    web:
      exposure:
        include: "*"

