server:
  port: 8082

spring:
  boot:
    admin:
      client:
        url: "http://localhost:9000"

management:
  endpoints:
    web:
      exposure:
        include: "*"
    jmx:
      exposure:
        include: "*"
  endpoint:
    web:
      exposure:
        include: "*"
  security:
    enabled: false

adminProperty: admin`s property value
userProperty: user`s property value
myProperty: my property value